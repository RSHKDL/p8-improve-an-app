{% extends 'base.html.twig' %}

{% block header_title %}<h1>{{ 'user.profile.title'|trans }}</h1>{% endblock %}

{% block body %}
    <div class="row">
        <ul>
            <li>{{ user.username }}</li>
            <li>{{ user.email }}</li>
        </ul>
        <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-success">{{ 'user.profile.edit'|trans }}</a>
    </div>
{% endblock %}
